# Copy to .workit.yml for repository-specific settings.
provider: auto

# Optional when the repository has multiple GitHub remotes.
# github:
#   repo: banyudu/example

worktree:
  branchPrefix: yudu
  baseBranch: auto
  directory: .worktrees

launch:
  target: banyan
  dependencies: symlink
  review: true

# These override the user-level agent configuration for this repository.
# default: codex
# agents:
#   claude:
#     provider: claude
#     weight: 3
#     command: claude --dangerously-skip-permissions
#   codex:
#     provider: codex
#     weight: 2
#     command: codex -p terra --dangerously-bypass-approvals-and-sandbox
#   # OpenCode agents — each maps to `opencode --agent <name>` (prompt via --prompt is handled automatically)
#   muse:
#     provider: opencode
#     weight: 0
#     command: opencode --agent muse-spark
#   mimo:
#     provider: opencode
#     weight: 0
#     command: opencode --agent mimo
#   hy:
#     provider: opencode
#     weight: 0
#     command: opencode --agent hy3
#   dpsk-flash:
#     provider: opencode
#     weight: 0
#     command: opencode --agent dpsk-v4-flash
#   dpsk-pro:
#     provider: opencode
#     weight: 0
#     command: opencode --agent dpsk-v4-pro
#   qwen:
#     provider: opencode
#     weight: 0
#     command: opencode --agent qwen
